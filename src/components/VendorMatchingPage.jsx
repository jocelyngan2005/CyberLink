import React from 'react';
import VendorMatchingDashboard from './VendorMatchingDashboard';

const VendorMatchingPage = () => {
  return (
    <VendorMatchingDashboard isOpen={true} onClose={() => {}} />
  );
};

export default VendorMatchingPage;